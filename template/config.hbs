<form autocomplete='off' class='flexcol'>
	{{#each settings}}
	<div class='form-group'>
		<label for='setting.onReady'>{{localize label}}</label>
		<div class='form-fields'>
			<input type='checkbox' name='{{key}}' data-dtype='Boolean' {{checked value}}></input>
			</input>
		</div>
		<p class='notes'>{{localize hint}}</p>
	</div>
	{{/each}}

	{{!-- BUTTONS --}}
	<div class='form-group'>
		<button type='submit' name='submit'>
			<i class="fas fa-save"></i>
			{{localize 'SETTINGS.Save'}}
		</button>
		<button type='submit' name='reset'>
			<i class="fas fa-sync"></i>
			{{localize 'SETTINGS.Reset'}}
		</button>
	</div>
</form>
